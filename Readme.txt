Firstly make sure that your input file is in the same folder as your executable

***compile and run**
- open terminal and type make to compile
- to execute, type ./huffencode inputfile outputfile

**Rules of use**
-use this format:
    ./huffencode [inputfile] [outputfile]
